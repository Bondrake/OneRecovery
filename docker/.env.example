# OneFileLinux Docker Build Configuration
# Copy this file to .env and modify as needed

# Build arguments to pass to the build.sh script
# Examples:
# BUILD_ARGS=--minimal
# BUILD_ARGS=--full
# BUILD_ARGS=--with-zfs --with-btrfs --with-recovery-tools
BUILD_ARGS=--with-zfs --with-recovery-tools --with-network-tools --use-cache

# User ID and Group ID mapping (defaults to current user)
# This ensures files created in the container have the correct ownership
# HOST_UID=1000
# HOST_GID=1000

# Docker resource limits (uncomment to enable)
# DOCKER_MEMORY=4g
# DOCKER_CPUS=4

# Advanced Docker configuration
# DOCKER_NETWORK=host
# DOCKER_EXTRA_HOSTS=somehost:127.0.0.1